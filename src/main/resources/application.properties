# Configuración de conexión a MariaDB para Spring Boot
spring.datasource.url=jdbc:mariadb://${MYSQLHOST}:${MYSQLPORT}/${MYSQL_DATABASE}?serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Configuración JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

#Configuración del JWTUtils
secretKey="vivigoeslamejorapp12345678904208302025"

# Configuración SMTP de Brevo
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=9b4704001@smtp-brevo.com
spring.mail.password=P7mcg0s28qxDrhNV
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true